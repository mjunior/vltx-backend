#!/usr/bin/env ruby
require_relative "../config/boot"
require_relative "../config/ci"

CI_STEPS.each do |label, command|
  puts "== #{label} =="
  success = system(command)
  exit($CHILD_STATUS ? $CHILD_STATUS.exitstatus : 1) unless success
end
