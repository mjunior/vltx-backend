---
phase: 17-idempotency-and-refund-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/db/migrate/20260308010000_add_wallet_refund_dedup_unique_index.rb
  - marketplace_backend/app/services/wallets/ledger/append_transaction.rb
  - marketplace_backend/app/services/wallets/operations/apply_movement.rb
  - marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb
  - marketplace_backend/test/services/wallets/operations/apply_movement_test.rb
autonomous: true
requirements:
  - WAL-05
  - IDEMP-01
  - IDEMP-02
user_setup: []
must_haves:
    truths:
      - Repeticao da mesma operacao logica retorna resultado idempotente de sucesso sem novo insert no ledger.
      - Refund duplicado por mesma referencia de negocio nao cria segunda transacao.
      - Mesma `operation_key` com payload divergente falha deterministicamente como conflito idempotente.
    artifacts:
      - path: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        provides: fluxo idempotente sob lock com comparacao de fingerprint e retorno da transacao existente.
      - path: marketplace_backend/db/migrate/20260308010000_add_wallet_refund_dedup_unique_index.rb
        provides: guarda deterministica no banco contra refund duplicado por referencia.
      - path: marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb
        provides: cobertura de retry idempotente, conflito de payload e dedup de refund.
    key_links:
      - from: marketplace_backend/app/services/wallets/operations/apply_movement.rb
        to: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        why: contrato idempotente deve ser uniforme no ponto unico de movimentacao financeira.
      - from: marketplace_backend/db/migrate/20260308010000_add_wallet_refund_dedup_unique_index.rb
        to: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        why: service e banco precisam convergir no mesmo criterio de deduplicacao de refund.
---

<objective>
Implementar a base de idempotencia forte da wallet com retorno deterministico de retry, deteccao de conflito por payload divergente e bloqueio de refund duplicado.

Purpose: Entregar WAL-05 e IDEMP-01 com garantias de consistencia sob concorrencia real.
Output: ajustes no engine ledger + constraint de banco + testes de contrato idempotente.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-idempotency-and-refund-deduplication/17-CONTEXT.md
@.planning/phases/17-idempotency-and-refund-deduplication/17-VALIDATION.md
@marketplace_backend/app/services/wallets/ledger/append_transaction.rb
@marketplace_backend/app/services/wallets/operations/apply_movement.rb
@marketplace_backend/app/models/wallet_transaction.rb
</context>

<tasks>

<task type="auto">
  <name>Implementar retorno idempotente para operation_key repetida</name>
  <files>marketplace_backend/app/services/wallets/ledger/append_transaction.rb, marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb</files>
  <action>Antes de inserir nova transacao, buscar sob lock por `wallet_id + operation_key`; se existir e fingerprint bater (tipo, amount, referencia, metadata normalizada), retornar sucesso com a transacao existente sem novo insert; se fingerprint divergir, retornar erro de conflito idempotente.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/ledger/append_transaction_test.rb</verify>
  <done>Retry com mesma chave nao duplica ledger e permanece deterministico, inclusive em mismatch de payload.</done>
</task>

<task type="auto">
  <name>Adicionar deduplicacao deterministica de refund por referencia</name>
  <files>marketplace_backend/db/migrate/20260308010000_add_wallet_refund_dedup_unique_index.rb, marketplace_backend/app/services/wallets/ledger/append_transaction.rb, marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb</files>
  <action>Criar indice unico parcial para `transaction_type='refund'` em (`wallet_id`, `reference_type`, `reference_id`) e tratar colisao no service retornando sucesso idempotente com refund existente quando fingerprint for equivalente.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/ledger/append_transaction_test.rb</verify>
  <done>Refund duplicado para mesma referencia nao gera segunda movimentacao financeira.</done>
</task>

<task type="auto">
  <name>Propagar contrato idempotente no service de operacao</name>
  <files>marketplace_backend/app/services/wallets/operations/apply_movement.rb, marketplace_backend/test/services/wallets/operations/apply_movement_test.rb</files>
  <action>Ajustar `ApplyMovement` para preservar semantica de sucesso idempotente e falha por conflito, sem expor caminhos inseguros de payload externo.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/operations/apply_movement_test.rb</verify>
  <done>Chamadores de movimentacao passam a receber resposta idempotente consistente em retries seguros.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Mesma `operation_key` com payload identico retorna sucesso sem novo insert
- [ ] Mesma `operation_key` com payload divergente retorna conflito idempotente
- [ ] Refund duplicado por referencia e bloqueado deterministicamente
- [ ] Service de operacao preserva contrato de seguranca sem confiar no FE
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Base idempotente pronta para prova de corridas concorrentes no plano 02
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
