---
phase: 01-user-and-profile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/db/migrate/*_create_users.rb
  - marketplace_backend/db/migrate/*_create_profiles.rb
  - marketplace_backend/app/models/user.rb
  - marketplace_backend/app/models/profile.rb
  - marketplace_backend/test/models/user_test.rb
  - marketplace_backend/test/models/profile_test.rb
autonomous: true
requirements:
  - AUTH-01
  - PROF-01
user_setup: []
must_haves:
  truths:
    - Signup foundation persists users with normalized unique email and secure password digest.
    - Every created user can own exactly one profile record via 1:1 association.
  artifacts:
    - path: marketplace_backend/app/models/user.rb
      provides: User model with secure password, normalization, validation, and has_one profile.
    - path: marketplace_backend/app/models/profile.rb
      provides: Profile model with belongs_to user and optional personal fields.
    - path: marketplace_backend/db/migrate/*_create_users.rb
      provides: users table with required authentication columns and indexes.
    - path: marketplace_backend/db/migrate/*_create_profiles.rb
      provides: profiles table with unique user reference and profile fields.
  key_links:
    - from: marketplace_backend/app/models/user.rb
      to: marketplace_backend/app/models/profile.rb
      why: User/Profile association must enforce 1:1 relationship.
    - from: marketplace_backend/test/models/user_test.rb
      to: marketplace_backend/app/models/user.rb
      why: Model tests lock security-critical validation behavior.
---

<objective>
Build the domain foundation for authentication identity and profile separation.

Purpose: Establish secure and explicit data boundaries before token lifecycle and endpoints.
Output: User/Profile models, migrations, and base model tests validating constraints.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-user-and-profile-foundation/01-CONTEXT.md
@.planning/phases/01-user-and-profile-foundation/01-RESEARCH.md
@marketplace_backend/app/models/application_record.rb
</context>

<tasks>

<task type="auto">
  <name>Create users and profiles schema with strict relational constraints</name>
  <files>marketplace_backend/db/migrate/*_create_users.rb, marketplace_backend/db/migrate/*_create_profiles.rb</files>
  <action>Create migrations for `users` and `profiles` with explicit nullability and relational integrity: users table with normalized-auth fields and profiles table keyed to user. Enforce one-to-one relation with unique index on `profiles.user_id`. Keep profile fields in scope to `full_name` and `photo_url` only.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails db:migrate:status</verify>
  <done>Migrations exist, are reversible, and reflect one-to-one user/profile relationship with no `address` column.</done>
</task>

<task type="auto">
  <name>Implement User and Profile models with security-aware validations</name>
  <files>marketplace_backend/app/models/user.rb, marketplace_backend/app/models/profile.rb</files>
  <action>Implement `User` with `has_secure_password`, email normalization (`strip.downcase`), case-insensitive uniqueness and minimum password length of 8. Implement `Profile` with `belongs_to :user` and optional `full_name`/`photo_url`. Keep auth and personal data separated per context decisions.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts [User.name, Profile.name].join(',')"</verify>
  <done>Models load correctly and include required association/validation behaviors for phase scope.</done>
</task>

<task type="auto">
  <name>Add model tests that lock core identity/profile invariants</name>
  <files>marketplace_backend/test/models/user_test.rb, marketplace_backend/test/models/profile_test.rb</files>
  <action>Add tests for email normalization, case-insensitive uniqueness, password length boundary, and one-to-one association semantics. Include negative cases that would break security expectations (duplicate email variant, invalid short password).</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/user_test.rb test/models/profile_test.rb</verify>
  <done>Model tests pass and clearly guard the invariants needed by later auth phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Database migrations run successfully for the phase scope
- [ ] Model test suite for User/Profile passes
- [ ] User/Profile schema and models match context decisions exactly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- User/Profile foundation supports secure signup work in subsequent plans
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
