---
phase: 01-user-and-profile-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - marketplace_backend/app/services/users/create.rb
  - marketplace_backend/test/services/users/create_test.rb
  - marketplace_backend/test/integration/auth_signup_test.rb
  - marketplace_backend/app/controllers/application_controller.rb
autonomous: true
requirements:
  - AUTH-01
  - PROF-02
user_setup: []
must_haves:
  truths:
    - Signup foundation creates User and Profile atomically with profile fields separated from auth credentials.
    - Sensitive signup failures (including existing email) respond with generic public message `cadastro inválido`.
  artifacts:
    - path: marketplace_backend/app/services/users/create.rb
      provides: Transaction-safe user/profile creation service with confirmation and normalization rules.
    - path: marketplace_backend/test/services/users/create_test.rb
      provides: Service tests for success and rollback/failure behavior.
    - path: marketplace_backend/test/integration/auth_signup_test.rb
      provides: Integration tests for signup policy and public error messaging.
  key_links:
    - from: marketplace_backend/app/services/users/create.rb
      to: marketplace_backend/app/models/user.rb
      why: Service must enforce model-level security and payload normalization.
    - from: marketplace_backend/test/integration/auth_signup_test.rb
      to: marketplace_backend/app/controllers/application_controller.rb
      why: Public error policy must remain generic and non-enumerating.
---

<objective>
Implement secure account creation foundation service and its verification contract.

Purpose: Ensure signup can be built safely on top of phase 1 domain without leaking account existence or breaking User/Profile separation.
Output: User creation service with tests for atomicity, profile initialization, and generic error behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-user-and-profile-foundation/01-CONTEXT.md
@.planning/phases/01-user-and-profile-foundation/01-RESEARCH.md
@.planning/phases/01-user-and-profile-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create transactional user signup service for User + Profile bootstrap</name>
  <files>marketplace_backend/app/services/users/create.rb</files>
  <action>Implement service object that receives signup payload, enforces required fields (`email`, `password`, `password_confirmation`), and creates `User` plus empty `Profile` in a transaction. Ensure failures return controlled error semantics without exposing account existence details.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts Users::Create.name"</verify>
  <done>Service exists, loads correctly, and performs atomic creation semantics aligned with context.</done>
</task>

<task type="auto">
  <name>Add service-level tests for success, rollback, and validation boundaries</name>
  <files>marketplace_backend/test/services/users/create_test.rb</files>
  <action>Add tests for happy path creation, missing confirmation, short password rejection, and failure rollback (profile/user consistency). Include checks that profile starts with nullable fields and no `address` handling in this phase.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/users/create_test.rb</verify>
  <done>Service tests pass and demonstrate expected atomic behavior and constraints.</done>
</task>

<task type="auto">
  <name>Define integration-level signup policy tests with generic error contract</name>
  <files>marketplace_backend/test/integration/auth_signup_test.rb, marketplace_backend/app/controllers/application_controller.rb</files>
  <action>Add integration tests that codify public response expectations for signup attempts, including duplicate-email path returning generic `cadastro inválido`. If needed, add minimal shared error renderer in `ApplicationController` to support consistent API error envelope used by later auth endpoints.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_signup_test.rb</verify>
  <done>Integration tests capture security messaging policy and pass consistently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Service test suite for signup foundation passes
- [ ] Integration tests for signup policy pass
- [ ] Signup foundation behavior remains generic for sensitive failures (`cadastro inválido`)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Signup foundation can support JWT endpoint work in phase 3 without policy changes
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
