---
phase: 12-cart-item-operations-with-server-side-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/cart_items_controller.rb
  - marketplace_backend/app/models/cart_item.rb
  - marketplace_backend/app/models/cart.rb
  - marketplace_backend/app/services/carts/add_item.rb
  - marketplace_backend/app/serializers/carts/cart_serializer.rb
  - marketplace_backend/db/migrate/20260307123000_create_cart_items.rb
  - marketplace_backend/test/integration/cart_items_create_test.rb
  - marketplace_backend/test/services/carts/add_item_test.rb
autonomous: true
requirements:
  - CART-03
  - CART-06
  - CART-07
  - CART-08
  - CART-09
user_setup: []
must_haves:
    truths:
      - Usuário autenticado adiciona item ao carrinho enviando apenas `product_id` e `quantity`.
      - Preço do item vem do produto no banco e não do payload do frontend.
      - Fluxo de add item é transacional e bloqueia produto próprio/inválido.
    artifacts:
      - path: marketplace_backend/app/services/carts/add_item.rb
        provides: regra de adição transacional com validações de estoque/produto/tenant.
      - path: marketplace_backend/app/controllers/cart_items_controller.rb
        provides: endpoint `POST /cart/items` com strong parameters mínimos.
      - path: marketplace_backend/test/integration/cart_items_create_test.rb
        provides: contrato HTTP para add item com payload permitido e erros de validação.
    key_links:
      - from: marketplace_backend/app/controllers/cart_items_controller.rb
        to: marketplace_backend/app/services/carts/add_item.rb
        why: controller deve delegar lógica de domínio ao service.
      - from: marketplace_backend/app/services/carts/add_item.rb
        to: marketplace_backend/app/models/product.rb
        why: preço e estoque devem ser derivados de `Product` persistido.
---

<objective>
Implementar a operação de adição de item no carrinho com validação server-side e sem confiar em preço/ids sensíveis do frontend.

Purpose: Entregar CART-03, CART-06, CART-07, CART-08 e CART-09 no fluxo de `add item`.
Output: endpoint `POST /cart/items` com service transacional e cobertura de contrato.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-cart-item-operations-with-server-side-validation/12-CONTEXT.md
@marketplace_backend/app/controllers/carts_controller.rb
@marketplace_backend/app/services/carts/find_or_create_active.rb
@marketplace_backend/app/models/product.rb
</context>

<tasks>

<task type="auto">
  <name>Criar estrutura de cart items e associação com carrinho/produto</name>
  <files>marketplace_backend/db/migrate/20260307123000_create_cart_items.rb, marketplace_backend/app/models/cart_item.rb, marketplace_backend/app/models/cart.rb</files>
  <action>Criar tabela `cart_items` com `cart_id`, `product_id`, `quantity`, índices de integridade e associação no `Cart` para compor subtotal server-side.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/cart_item_test.rb</verify>
  <done>Domínio de item de carrinho existe com integridade relacional para operações da fase 12.</done>
</task>

<task type="auto">
  <name>Implementar service transacional para adicionar item com validações de domínio</name>
  <files>marketplace_backend/app/services/carts/add_item.rb, marketplace_backend/app/serializers/carts/cart_serializer.rb</files>
  <action>Implementar fluxo de add item que valida `product_id` e `quantity`, deriva preço do `Product`, aplica clamp por estoque, bloqueia produto próprio e persiste com transação atômica.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/carts/add_item_test.rb</verify>
  <done>Adição de item fica robusta contra fraude e concorrência, com subtotal recalculado no backend.</done>
</task>

<task type="auto">
  <name>Expor endpoint POST /cart/items com contrato mínimo e testes de integração</name>
  <files>marketplace_backend/config/routes.rb, marketplace_backend/app/controllers/cart_items_controller.rb, marketplace_backend/test/integration/cart_items_create_test.rb</files>
  <action>Criar endpoint autenticado que aceita apenas `{ cart_item: { product_id, quantity } }`, ignora `price` no payload e retorna contrato consistente de carrinho atualizado.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/cart_items_create_test.rb test/services/carts/add_item_test.rb</verify>
  <done>Contrato HTTP de add item fica estável e coberto por regressão automatizada.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Add item aceita somente `product_id` e `quantity`
- [ ] Preço nunca vem do payload e subtotal é recalculado server-side
- [ ] Produto próprio/inativo/deletado e IDs inválidos não entram no carrinho
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Base de cart items pronta para update/remove
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
