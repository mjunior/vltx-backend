---
phase: 18-wallet-authorization-and-tenant-isolation-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/wallets_controller.rb
  - marketplace_backend/app/services/wallets/read/fetch_balance.rb
  - marketplace_backend/app/services/wallets/read/fetch_statement.rb
  - marketplace_backend/app/serializers/wallets/balance_serializer.rb
  - marketplace_backend/app/serializers/wallets/statement_transaction_serializer.rb
  - marketplace_backend/test/integration/wallet_authorization_test.rb
  - marketplace_backend/test/services/wallets/read/fetch_statement_test.rb
autonomous: true
requirements:
  - AUTHZ-08
  - AUTHZ-09
user_setup: []
must_haves:
    truths:
      - Endpoints de wallet sempre operam no contexto de `current_user`, sem aceitar `wallet_id` de entrada.
      - `GET /wallet/transactions` retorna sempre as 30 transacoes mais recentes, sem filtros/paginacao.
      - Response de extrato nao expoe `operation_key` nem `metadata`.
    artifacts:
      - path: marketplace_backend/app/controllers/wallets_controller.rb
        provides: boundary HTTP autenticado com ownership derivado do token.
      - path: marketplace_backend/app/services/wallets/read/fetch_statement.rb
        provides: leitura hardcoded de ultimas 30 transacoes com ordenacao segura.
      - path: marketplace_backend/test/integration/wallet_authorization_test.rb
        provides: evidencias request-level de authz e isolamento tenant.
    key_links:
      - from: marketplace_backend/app/controllers/wallets_controller.rb
        to: marketplace_backend/app/services/wallets/read/fetch_balance.rb
        why: controller deve delegar regra de ownership e shape de leitura para service dedicado.
      - from: marketplace_backend/app/services/wallets/read/fetch_statement.rb
        to: marketplace_backend/app/models/wallet_transaction.rb
        why: reuso do scope `recent_first` garante contrato de ordenacao e limite fixo.
---

<objective>
Implementar o surface de leitura da wallet do usuario autenticado com isolamento tenant estrito e contrato fixo de extrato.

Purpose: Entregar AUTHZ-08 e base principal de AUTHZ-09 no boundary HTTP de wallet.
Output: rotas/controller/services/serializers de wallet + testes iniciais de autorizacao.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-wallet-authorization-and-tenant-isolation-surface/18-CONTEXT.md
@.planning/phases/18-wallet-authorization-and-tenant-isolation-surface/18-VALIDATION.md
@marketplace_backend/app/controllers/application_controller.rb
@marketplace_backend/app/models/wallet.rb
@marketplace_backend/app/models/wallet_transaction.rb
</context>

<tasks>

<task type="auto">
  <name>Criar endpoints privados de leitura da wallet</name>
  <files>marketplace_backend/config/routes.rb, marketplace_backend/app/controllers/wallets_controller.rb, marketplace_backend/app/services/wallets/read/fetch_balance.rb</files>
  <action>Adicionar `GET /wallet` e `GET /wallet/transactions` protegidos por `authenticate_user!`, sempre derivando wallet por `current_user` com auto-provision quando ausente e sem aceitar IDs de wallet no contrato.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/wallet_authorization_test.rb</verify>
  <done>Usuario autenticado consegue consultar apenas a propria wallet, sem surface para wallet de terceiros.</done>
</task>

<task type="auto">
  <name>Implementar leitura de extrato com contrato hardcoded de 30 registros</name>
  <files>marketplace_backend/app/services/wallets/read/fetch_statement.rb, marketplace_backend/app/serializers/wallets/statement_transaction_serializer.rb, marketplace_backend/test/services/wallets/read/fetch_statement_test.rb</files>
  <action>Implementar leitura de `wallet_transactions.recent_first.limit(30)` e serializacao restrita aos campos permitidos (`id`, `transaction_type`, `amount_cents`, `balance_after_cents`, `created_at`, `reference_type`, `reference_id`), sem expor `operation_key`/`metadata` e sem filtros/paginacao.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/read/fetch_statement_test.rb</verify>
  <done>Extrato retorna sempre as ultimas 30 transacoes no formato seguro definido na fase.</done>
</task>

<task type="auto">
  <name>Adicionar testes de autenticacao e contrato de erro base</name>
  <files>marketplace_backend/test/integration/wallet_authorization_test.rb</files>
  <action>Cobrir `401 token invalido` sem token, sucesso de leitura para owner e validacao de que payload/query indevidos nao alteram ownership nem retornam dados sensiveis fora do contrato.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/wallet_authorization_test.rb</verify>
  <done>Boundary HTTP de wallet fica coberto para auth basica e contrato de erro padronizado.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Endpoints de wallet operam apenas via identidade do token
- [ ] Extrato retorna sempre ultimas 30 transacoes (sem filtros/paginacao)
- [ ] `operation_key` e `metadata` nao aparecem no response
- [ ] Falta de token retorna `401 token invalido`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Surface de leitura da wallet pronto para hardening de tenant-isolation no plano 02
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
