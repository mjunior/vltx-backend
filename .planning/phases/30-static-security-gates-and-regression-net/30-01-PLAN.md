---
phase: 30-static-security-gates-and-regression-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/bin/security
  - marketplace_backend/config/ci.rb
  - .github/workflows/ci.yml
  - marketplace_backend/README.md
autonomous: true
requirements:
  - SEC-03
user_setup: []
must_haves:
  truths:
    - Existe um entrypoint único e fail-closed para validação estática de segurança.
    - O workflow do repositório executa esse gate de forma automática.
    - A documentação local aponta para o mesmo comando usado em CI, sem drift.
  artifacts:
    - path: marketplace_backend/bin/security
      provides: comando único para `bundler-audit` e `brakeman`
    - path: .github/workflows/ci.yml
      provides: workflow automatizado que executa o gate e a suíte principal
    - path: marketplace_backend/config/ci.rb
      provides: reutilização do novo gate no fluxo local de CI
  key_links:
    - from: marketplace_backend/bin/security
      to: marketplace_backend/bin/bundler-audit
      why: o gate deve reutilizar a configuração existente de advisories/ignore list
    - from: .github/workflows/ci.yml
      to: marketplace_backend/config/ci.rb
      why: o workflow remoto precisa seguir a mesma política do comando local
---

<objective>
Consolidar um gate estático único e obrigatório para segurança, compartilhado entre uso local e CI do repositório.

Purpose: satisfazer `SEC-03` fechando o gap entre ferramentas existentes e enforcement real.
Output: binstub único de segurança, workflow do repositório e documentação enxuta do fluxo obrigatório.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-static-security-gates-and-regression-net/30-CONTEXT.md
@.planning/phases/30-static-security-gates-and-regression-net/30-VALIDATION.md
@marketplace_backend/config/ci.rb
@marketplace_backend/bin/bundler-audit
@marketplace_backend/bin/brakeman
</context>

<tasks>

<task type="auto">
  <name>Criar gate único fail-closed para validação estática de segurança</name>
  <files>marketplace_backend/bin/security, marketplace_backend/config/ci.rb, marketplace_backend/README.md</files>
  <action>Criar um comando único que execute `bundler-audit` e `brakeman` em modo obrigatório, atualizar `config/ci.rb` para reutilizá-lo e documentar o fluxo local esperado sem duplicar comandos longos.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec ./bin/security</verify>
  <done>O projeto passa a ter um entrypoint claro e único para security checks estáticos.</done>
</task>

<task type="auto">
  <name>Integrar o gate único ao workflow automatizado do repositório</name>
  <files>.github/workflows/ci.yml, marketplace_backend/config/ci.rb, marketplace_backend/README.md</files>
  <action>Criar um workflow de CI que instale Ruby/dependências, execute o gate estático e a suíte principal usando os entrypoints do repo, minimizando drift entre local e remoto.</action>
  <verify>ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/ci.yml\")' && cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec ./bin/ci</verify>
  <done>O gate deixa de ser apenas local e passa a existir como enforcement automatizado de repositório.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] existe um comando único para `bundler-audit` + `brakeman`
- [ ] o comando falha quando qualquer etapa falha
- [ ] o workflow do repositório executa o mesmo gate
- [ ] documentação local aponta para o mesmo fluxo
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Static security checks are centrally invoked and CI-enforced
- Local and remote workflows share the same entrypoints
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
