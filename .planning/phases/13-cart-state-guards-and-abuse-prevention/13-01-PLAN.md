---
phase: 13-cart-state-guards-and-abuse-prevention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/app/controllers/cart_items_controller.rb
  - marketplace_backend/app/services/carts/add_item.rb
  - marketplace_backend/app/services/carts/update_item.rb
  - marketplace_backend/app/services/carts/remove_item.rb
  - marketplace_backend/app/services/carts/inactive_cart_abuse_guard.rb
  - marketplace_backend/test/services/carts/add_item_test.rb
  - marketplace_backend/test/services/carts/update_item_test.rb
  - marketplace_backend/test/services/carts/remove_item_test.rb
  - marketplace_backend/test/services/carts/inactive_cart_abuse_guard_test.rb
autonomous: true
requirements:
  - AUTHZ-07
user_setup: []
must_haves:
    truths:
      - Mutações de item em carrinho `finished`/`abandoned` retornam erro genérico `422 payload invalido` sem expor estado real.
      - `PATCH/DELETE` sem carrinho ativo continuam retornando `404 nao encontrado`.
      - Tentativas repetidas de mutação em carrinho inativo disparam proteção de sessão/token e log estruturado por ação.
    artifacts:
      - path: marketplace_backend/app/services/carts/inactive_cart_abuse_guard.rb
        provides: política de limiar para abuso com revogação de sessão via domínio auth.
      - path: marketplace_backend/app/services/carts/add_item.rb
        provides: bloqueio explícito de mutação quando carrinho alvo está inativo.
      - path: marketplace_backend/app/controllers/cart_items_controller.rb
        provides: mapeamento HTTP consistente (`422 payload invalido` e `404 nao encontrado`).
    key_links:
      - from: marketplace_backend/app/services/carts/add_item.rb
        to: marketplace_backend/app/services/carts/inactive_cart_abuse_guard.rb
        why: add item deve registrar tentativa em carrinho inativo e acionar revogação ao atingir limiar.
      - from: marketplace_backend/app/services/carts/update_item.rb
        to: marketplace_backend/app/services/auth/sessions/revoke_all.rb
        why: tentativa repetida em estado inválido precisa endurecer sessão/token no backend.
---

<objective>
Aplicar guardas de estado ativo em todos os services de mutação de cart item e embutir proteção anti-abuso com logging estruturado.

Purpose: Entregar AUTHZ-07 no nível de domínio/serviço sem depender de validação do frontend.
Output: serviços de item endurecidos, política de abuso integrada ao auth e contratos HTTP consistentes.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-cart-state-guards-and-abuse-prevention/13-CONTEXT.md
@.planning/phases/13-cart-state-guards-and-abuse-prevention/13-VALIDATION.md
@marketplace_backend/app/services/carts/cart_finder.rb
@marketplace_backend/app/services/auth/sessions/revoke_all.rb
</context>

<tasks>

<task type="auto">
  <name>Aplicar guarda de estado ativo em add/update/remove com contrato uniforme</name>
  <files>marketplace_backend/app/services/carts/add_item.rb, marketplace_backend/app/services/carts/update_item.rb, marketplace_backend/app/services/carts/remove_item.rb</files>
  <action>Garantir que mutações só ocorram com carrinho `active`: bloquear cenários de carrinho inativo com `:invalid_payload`, manter `:not_found` para update/delete quando não há carrinho ativo, e não reabrir carrinhos `finished/abandoned`.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/carts/add_item_test.rb test/services/carts/update_item_test.rb test/services/carts/remove_item_test.rb</verify>
  <done>Regra de estado ativo fica centralizada e consistente em todos os fluxos de mutação.</done>
</task>

<task type="auto">
  <name>Implementar anti-abuso por tentativa repetida em carrinho inativo</name>
  <files>marketplace_backend/app/services/carts/inactive_cart_abuse_guard.rb, marketplace_backend/app/services/carts/add_item.rb, marketplace_backend/app/services/carts/update_item.rb, marketplace_backend/app/services/carts/remove_item.rb, marketplace_backend/test/services/carts/inactive_cart_abuse_guard_test.rb</files>
  <action>Criar guard dedicado com contador por usuário/ação (ex.: cache), emitir logs estruturados (`user_id`, `cart_id`, `status`, `action`) e acionar `Auth::Sessions::RevokeAll` ao atingir limiar de abuso definido para a fase.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/carts/inactive_cart_abuse_guard_test.rb</verify>
  <done>Abuso repetido em carrinho inativo gera resposta de segurança e trilha de auditoria sem vazar estado interno.</done>
</task>

<task type="auto">
  <name>Alinhar controller ao contrato de erro da fase 13</name>
  <files>marketplace_backend/app/controllers/cart_items_controller.rb, marketplace_backend/test/integration/cart_items_create_test.rb, marketplace_backend/test/integration/cart_items_update_test.rb, marketplace_backend/test/integration/cart_items_destroy_test.rb</files>
  <action>Mapear `error_code` dos services para garantir `422 payload invalido` em estado inválido e `404 nao encontrado` apenas em cenários sem recurso ativo no escopo tenant (update/delete sem carrinho ativo).</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/cart_items_create_test.rb test/integration/cart_items_update_test.rb test/integration/cart_items_destroy_test.rb</verify>
  <done>Contrato HTTP fica previsível para frontend e aderente à política de não exposição de estado.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `finished`/`abandoned` nunca sofrem mutação de item
- [ ] Erro de estado inválido retorna sempre `422 payload invalido`
- [ ] Sem carrinho ativo em update/delete retorna `404 nao encontrado`
- [ ] Abuso repetido aciona revogação de sessão/token com logs estruturados
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Base de proteção de estado/abuso pronta para finalização do carrinho na fase 14
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
