---
phase: 02-jwt-and-session-security-core
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - marketplace_backend/app/services/auth/sessions/token_digest.rb
  - marketplace_backend/app/services/auth/sessions/find_active_session.rb
  - marketplace_backend/app/services/auth/sessions/revoke_all.rb
  - marketplace_backend/app/services/auth/sessions/detect_reuse.rb
  - marketplace_backend/test/services/auth/sessions/revocation_test.rb
autonomous: true
requirements:
  - SESS-06
user_setup: []
must_haves:
  truths:
    - Validação de `jti` consulta estado persistido e recusa sessão revogada/inexistente/expirada.
    - Reuso de refresh revogado aciona revogação global de sessões do usuário.
  artifacts:
    - path: marketplace_backend/app/services/auth/sessions/find_active_session.rb
      provides: Checagem consistente de sessão ativa por `jti` + hash + expiração.
    - path: marketplace_backend/app/services/auth/sessions/revoke_all.rb
      provides: Revogação global em lote para incidentes e logout.
    - path: marketplace_backend/app/services/auth/sessions/detect_reuse.rb
      provides: Detecção explícita de reuse com acionamento de contenção.
  key_links:
    - from: marketplace_backend/app/services/auth/jwt/verifier.rb
      to: marketplace_backend/app/services/auth/sessions/find_active_session.rb
      why: `jti` assinado precisa ser conferido contra estado de sessão para efetivar revogação.
    - from: marketplace_backend/app/services/auth/sessions/detect_reuse.rb
      to: marketplace_backend/app/services/auth/sessions/revoke_all.rb
      why: Política de segurança exige logout global ao detectar reuse revogado.
---

<objective>
Implementar os serviços de revogação e checagem por `jti` que sustentam refresh one-time e resposta a incidente.

Purpose: Tornar verificável em código a política de sessão revogável definida na fase.
Output: Utilitários de hash+pepper, busca de sessão ativa, revogação global e detecção de reuse.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-jwt-and-session-security-core/02-CONTEXT.md
@.planning/phases/02-jwt-and-session-security-core/02-RESEARCH.md
@.planning/phases/02-jwt-and-session-security-core/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Implementar utilitário de digest de refresh token com SHA-256 + pepper</name>
  <files>marketplace_backend/app/services/auth/sessions/token_digest.rb</files>
  <action>Criar utilitário puro para derivar hash de refresh token usando `SHA-256(token + JWT_REFRESH_PEPPER)` com API simples e determinística para uso em create/refresh/check.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts Auth::Sessions::TokenDigest.call('abc').size"</verify>
  <done>Digest é estável para mesma entrada e muda ao alterar pepper/token.</done>
</task>

<task type="auto">
  <name>Implementar serviços de checagem ativa por jti e revogação global</name>
  <files>marketplace_backend/app/services/auth/sessions/find_active_session.rb, marketplace_backend/app/services/auth/sessions/revoke_all.rb</files>
  <action>Implementar busca segura da sessão ativa (jti + hash + status + expiração) e serviço de revogação global que marca `revoked_at` em todas sessões ativas do usuário com operação idempotente.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/auth/sessions/revocation_test.rb</verify>
  <done>Serviços localizam somente sessão válida e revogam globalmente sem efeitos colaterais indevidos.</done>
</task>

<task type="auto">
  <name>Implementar detecção de reuse e testes de política de incidente</name>
  <files>marketplace_backend/app/services/auth/sessions/detect_reuse.rb, marketplace_backend/test/services/auth/sessions/revocation_test.rb</files>
  <action>Adicionar rotina que identifica tentativa com refresh revogado/rotacionado e dispara `revoke_all` do usuário comprometido, retornando resultado de domínio consumível por fluxo de refresh/logout das próximas fases.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/auth/sessions/revocation_test.rb</verify>
  <done>Teste prova que reuse revogado causa revogação global e respostas consistentes para tokens inválidos.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Checagem por `jti` + hash + expiração implementada
- [ ] Revogação global de sessões ativa e idempotente
- [ ] Detecção de reuse disparando contenção global coberta por testes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Núcleo de sessão revogável pronto para endpoints da fase 3
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
