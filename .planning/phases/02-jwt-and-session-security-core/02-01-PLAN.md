---
phase: 02-jwt-and-session-security-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/Gemfile
  - marketplace_backend/config/initializers/jwt.rb
  - marketplace_backend/app/services/auth/jwt/config.rb
  - marketplace_backend/app/services/auth/jwt/issuer.rb
  - marketplace_backend/app/services/auth/jwt/verifier.rb
  - marketplace_backend/app/services/auth/jwt/errors.rb
  - marketplace_backend/test/services/auth/jwt/issuer_test.rb
  - marketplace_backend/test/services/auth/jwt/verifier_test.rb
autonomous: true
requirements:
  - AUTH-05
  - SESS-06
user_setup:
  - Definir ENV de secrets para desenvolvimento/teste: `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `JWT_REFRESH_PEPPER`.
must_haves:
  truths:
    - Emissão e validação de access/refresh usam segredos distintos e obrigatórios.
    - Tokens carregam `jti` e `type` obrigatório para validação contextual.
  artifacts:
    - path: marketplace_backend/config/initializers/jwt.rb
      provides: Fail-fast de configuração e carregamento seguro de secrets/pepper.
    - path: marketplace_backend/app/services/auth/jwt/issuer.rb
      provides: Emissão de tokens com claims mínimos e TTL configurável.
    - path: marketplace_backend/app/services/auth/jwt/verifier.rb
      provides: Verificação estrita de assinatura, expiração e tipo esperado.
  key_links:
    - from: marketplace_backend/app/services/auth/jwt/config.rb
      to: marketplace_backend/config/initializers/jwt.rb
      why: Config central deve ser validada no boot, não em runtime tardio.
    - from: marketplace_backend/test/services/auth/jwt/verifier_test.rb
      to: marketplace_backend/app/services/auth/jwt/verifier.rb
      why: Testes devem bloquear regressão em claims obrigatórios (`jti`, `type`).
---

<objective>
Implementar o núcleo JWT da aplicação com separação forte de segredos e verificação robusta para access/refresh.

Purpose: Garantir base criptográfica segura antes de expor endpoints de login/refresh.
Output: Serviços de issue/verify com claims mínimos, validações de tipo e configuração fail-fast.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-jwt-and-session-security-core/02-CONTEXT.md
@.planning/phases/02-jwt-and-session-security-core/02-RESEARCH.md
@marketplace_backend/Gemfile
</context>

<tasks>

<task type="auto">
  <name>Adicionar gem JWT e configuração obrigatória de segredos/pepper</name>
  <files>marketplace_backend/Gemfile, marketplace_backend/config/initializers/jwt.rb, marketplace_backend/app/services/auth/jwt/config.rb</files>
  <action>Adicionar `gem "jwt"`. Criar configuração central de autenticação JWT que lê `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET` e `JWT_REFRESH_PEPPER`, valida presença e garante que access/refresh secrets são distintos. Levantar erro no boot se configuração estiver inválida.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts Auth::Jwt::Config.algorithm"</verify>
  <done>App sobe com configuração válida e falha imediatamente quando algum segredo obrigatório está ausente.</done>
</task>

<task type="auto">
  <name>Implementar serviço de emissão de JWT com claims mínimos e tipo explícito</name>
  <files>marketplace_backend/app/services/auth/jwt/issuer.rb, marketplace_backend/app/services/auth/jwt/errors.rb</files>
  <action>Implementar emissor para `access` e `refresh` com claims mínimos (`sub`, `jti`, `type`, `iat`, `exp`) e `HS256`. Garantir geração de `jti` único por token e APIs explícitas para cada tipo.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/auth/jwt/issuer_test.rb</verify>
  <done>Issuer retorna token assinado e payload esperado para ambos os tipos com TTL correto.</done>
</task>

<task type="auto">
  <name>Implementar verificador JWT com validação de assinatura, expiração e tipo</name>
  <files>marketplace_backend/app/services/auth/jwt/verifier.rb, marketplace_backend/test/services/auth/jwt/verifier_test.rb</files>
  <action>Criar verificador que decodifica token com secret correto por tipo, exige claim `jti`, valida `type` esperado e encapsula falhas em erro de domínio sem vazar detalhes ao controller.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/auth/jwt/verifier_test.rb</verify>
  <done>Tokens inválidos/revogados malformados são rejeitados de forma consistente e testada.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Segredos JWT distintos e pepper obrigatórios validados no boot
- [ ] Emissão de tokens com claims mínimos incluindo `jti`
- [ ] Verificação por tipo rejeita token inválido de forma consistente
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Foundation de JWT pronta para persistência de sessão na sequência
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
