---
phase: 02-jwt-and-session-security-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/db/migrate/*_create_refresh_sessions.rb
  - marketplace_backend/app/models/refresh_session.rb
  - marketplace_backend/app/models/user.rb
  - marketplace_backend/test/models/refresh_session_test.rb
autonomous: true
requirements:
  - SESS-01
user_setup: []
must_haves:
  truths:
    - Refresh token não é persistido em texto puro, apenas hash + metadados revogáveis.
    - Cada refresh `jti` persistido pode ser auditado e invalidado por estado de sessão.
  artifacts:
    - path: marketplace_backend/db/migrate/*_create_refresh_sessions.rb
      provides: Estrutura relacional para sessão de refresh com hash, jti e expiração.
    - path: marketplace_backend/app/models/refresh_session.rb
      provides: Regras de estado e validações de integridade para sessão revogável.
    - path: marketplace_backend/test/models/refresh_session_test.rb
      provides: Cobertura de invariantes de persistência segura e transições de estado.
  key_links:
    - from: marketplace_backend/app/models/refresh_session.rb
      to: marketplace_backend/app/models/user.rb
      why: Revogação global depende de associação robusta sessão→usuário.
---

<objective>
Persistir sessão de refresh de forma segura e auditável para suportar rotação one-time e revogação.

Purpose: Construir estado de sessão necessário para segurança de refresh token.
Output: Migration + model `RefreshSession` com hash, `jti`, expiração e marcadores de revogação/rotação.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-jwt-and-session-security-core/02-CONTEXT.md
@.planning/phases/02-jwt-and-session-security-core/02-RESEARCH.md
@marketplace_backend/db/schema.rb
</context>

<tasks>

<task type="auto">
  <name>Criar migration de refresh_sessions com constraints e índices de segurança</name>
  <files>marketplace_backend/db/migrate/*_create_refresh_sessions.rb</files>
  <action>Criar tabela `refresh_sessions` com `user_id`, `refresh_jti`, `refresh_token_hash`, `expires_at`, `revoked_at`, `rotated_at` e timestamps. Aplicar `null: false` onde necessário, índice único em `refresh_jti` e índices úteis para revogação por usuário.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails db:migrate:status</verify>
  <done>Migração aplicada e schema reflete exatamente os campos mínimos aprovados para a fase.</done>
</task>

<task type="auto">
  <name>Implementar modelo RefreshSession com validações e helpers de estado</name>
  <files>marketplace_backend/app/models/refresh_session.rb, marketplace_backend/app/models/user.rb</files>
  <action>Implementar `RefreshSession` com `belongs_to :user`, validações de presença e métodos de estado (`revoked?`, `expired?`, `active?`). Adicionar associação em `User` para suportar revogação global futura.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts RefreshSession.new.respond_to?(:active?)"</verify>
  <done>Modelo expõe regras de estado confiáveis e associação correta com usuário.</done>
</task>

<task type="auto">
  <name>Adicionar testes de modelo para persistência segura e estados de revogação</name>
  <files>marketplace_backend/test/models/refresh_session_test.rb</files>
  <action>Escrever testes para presença de campos críticos, unicidade de `refresh_jti`, transições de estado ativo/revogado/expirado e garantia de ausência de coluna plaintext token.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/refresh_session_test.rb</verify>
  <done>Testes do modelo passam e blindam invariantes de `SESS-01`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Tabela `refresh_sessions` criada com constraints e índices corretos
- [ ] Modelo `RefreshSession` representa estado revogável/expirável
- [ ] Persistência não usa refresh token plaintext
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Base de sessão pronta para rotação e revogação transacional
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
