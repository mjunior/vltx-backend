---
phase: 23-admin-identity-boundary-and-verification-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/db/migrate/*_create_admins.rb
  - marketplace_backend/db/migrate/*_create_admin_refresh_sessions.rb
  - marketplace_backend/db/migrate/*_add_verification_status_to_users.rb
  - marketplace_backend/app/models/admin.rb
  - marketplace_backend/app/models/admin_refresh_session.rb
  - marketplace_backend/app/models/user.rb
  - marketplace_backend/app/services/admin_auth/jwt/config.rb
  - marketplace_backend/test/models/admin_test.rb
  - marketplace_backend/test/models/admin_refresh_session_test.rb
  - marketplace_backend/test/models/user_test.rb
  - marketplace_backend/test/test_helper.rb
autonomous: true
requirements:
  - ADM-01
  - USR-01
user_setup: []
must_haves:
  truths:
    - Admin é uma entidade própria e não deriva de `User`.
    - Sessões admin são persistidas separadamente das refresh sessions de usuário.
    - Todo usuário novo nasce como `unverified`.
  artifacts:
    - path: marketplace_backend/app/models/admin.rb
      provides: domínio administrativo separado do usuário comum
    - path: marketplace_backend/app/models/admin_refresh_session.rb
      provides: persistência segregada de sessão refresh para admin
    - path: marketplace_backend/app/models/user.rb
      provides: status de verificação base para o restante do milestone
  key_links:
    - from: marketplace_backend/app/models/admin_refresh_session.rb
      to: marketplace_backend/app/models/admin.rb
      why: refresh sessions administrativas devem pertencer a admins, não a users
    - from: marketplace_backend/app/services/admin_auth/jwt/config.rb
      to: marketplace_backend/test/test_helper.rb
      why: secrets admin precisam existir desde o ambiente de teste para estabilizar a fase
---

<objective>
Criar a fundação de dados e configuração para identidade admin separada e status de verificação de usuário.

Purpose: estabelecer as entidades e variáveis de configuração que isolam o domínio administrativo antes de expor endpoints.
Output: schema/models de `Admin` e `AdminRefreshSession`, config JWT admin e `verification_status` em `User`.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-admin-identity-boundary-and-verification-foundation/23-CONTEXT.md
@marketplace_backend/app/models/user.rb
@marketplace_backend/app/services/auth/jwt/config.rb
</context>

<tasks>

<task type="auto">
  <name>Criar schema e modelos de Admin e refresh session segregada</name>
  <files>marketplace_backend/db/migrate/*_create_admins.rb, marketplace_backend/db/migrate/*_create_admin_refresh_sessions.rb, marketplace_backend/app/models/admin.rb, marketplace_backend/app/models/admin_refresh_session.rb, marketplace_backend/test/models/admin_test.rb, marketplace_backend/test/models/admin_refresh_session_test.rb</files>
  <action>Adicionar entidade `Admin` com `email`, `password_digest` e controle de ativo, além de uma tabela própria de refresh session administrativa com `refresh_jti`, `refresh_token_hash`, `expires_at`, `revoked_at` e `rotated_at`, sem compartilhar persistência com `User`.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/admin_test.rb test/models/admin_refresh_session_test.rb</verify>
  <done>Domínio administrativo existe de forma isolada e com storage de sessão próprio.</done>
</task>

<task type="auto">
  <name>Adicionar status de verificação base ao User</name>
  <files>marketplace_backend/db/migrate/*_add_verification_status_to_users.rb, marketplace_backend/app/models/user.rb, marketplace_backend/test/models/user_test.rb</files>
  <action>Adicionar `verification_status` ao `User` com enum segura `unverified`/`verified`, default `unverified`, validações explícitas e cobertura de modelo garantindo que novos usuários nunca nascem verificados por acidente.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/user_test.rb</verify>
  <done>Status de verificação fica persistido e pronto para uso administrativo futuro.</done>
</task>

<task type="auto">
  <name>Configurar secrets e TTLs do JWT admin para teste e app</name>
  <files>marketplace_backend/app/services/admin_auth/jwt/config.rb, marketplace_backend/test/test_helper.rb</files>
  <action>Criar configuração de JWT admin paralela ao fluxo de usuário, com secrets obrigatórios e distintos para access, refresh e pepper, além de defaults controlados em desenvolvimento/teste para permitir a futura suíte de auth administrativa.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails runner "puts AdminAuth::Jwt::Config.access_secret.present? && AdminAuth::Jwt::Config.refresh_secret.present? && AdminAuth::Jwt::Config.refresh_pepper.present?"</verify>
  <done>Configuração criptográfica admin está pronta e fail-fast sem contaminar o fluxo atual de usuário.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Admin` e `AdminRefreshSession` existem separados de `User`
- [ ] `User` nasce `unverified` por padrão
- [ ] config JWT admin exige secrets próprios
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No user auth table or config was repurposed to impersonate admin auth
- Verification status foundation is ready for admin-only exposure later in the phase
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
