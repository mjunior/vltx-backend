---
phase: 11-cart-foundation-and-active-cart-invariant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/carts_controller.rb
  - marketplace_backend/app/models/cart.rb
  - marketplace_backend/app/services/carts/find_or_create_active.rb
  - marketplace_backend/app/serializers/carts/cart_serializer.rb
  - marketplace_backend/db/migrate/20260307110000_create_carts.rb
  - marketplace_backend/test/integration/cart_upsert_test.rb
  - marketplace_backend/test/services/carts/find_or_create_active_test.rb
autonomous: true
requirements:
  - CART-01
  - AUTHZ-05
user_setup: []
must_haves:
    truths:
      - Apenas usuário autenticado consegue obter/criar carrinho ativo.
      - `POST /cart` cria carrinho ativo quando não existe e retorna envelope `{ data: { cart } }`.
      - Requisição repetida de `POST /cart` para o mesmo usuário reutiliza o mesmo carrinho ativo.
    artifacts:
      - path: marketplace_backend/app/controllers/carts_controller.rb
        provides: endpoint privado com strong parameters e delegação total ao service.
      - path: marketplace_backend/app/services/carts/find_or_create_active.rb
        provides: regra de domínio para obter/criar carrinho ativo por usuário autenticado.
      - path: marketplace_backend/test/integration/cart_upsert_test.rb
        provides: cobertura request-level para autenticação obrigatória e contrato do endpoint.
    key_links:
      - from: marketplace_backend/app/controllers/carts_controller.rb
        to: marketplace_backend/app/services/carts/find_or_create_active.rb
        why: controller deve permanecer fino e sem lógica de domínio.
      - from: marketplace_backend/app/services/carts/find_or_create_active.rb
        to: marketplace_backend/app/models/cart.rb
        why: serviço precisa aplicar regra de carrinho ativo por usuário com escopo tenant.
---

<objective>
Implementar a fundação do carrinho ativo com endpoint privado e criação idempotente por usuário autenticado.

Purpose: Cumprir CART-01 e AUTHZ-05 sem confiar em identidade vinda do frontend.
Output: `POST /cart` funcionando com autenticação obrigatória e contrato estável para obter/criar carrinho ativo.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-cart-foundation-and-active-cart-invariant/11-CONTEXT.md
@marketplace_backend/app/controllers/application_controller.rb
@marketplace_backend/app/controllers/products_controller.rb
@marketplace_backend/app/services/products/create.rb
</context>

<tasks>

<task type="auto">
  <name>Criar modelo de carrinho e rota privada de upsert do carrinho ativo</name>
  <files>marketplace_backend/db/migrate/20260307110000_create_carts.rb, marketplace_backend/app/models/cart.rb, marketplace_backend/config/routes.rb</files>
  <action>Adicionar tabela `carts` com `user_id` e `status`, definir associação com `User`, e criar rota `POST /cart` para fluxo privado de carrinho ativo.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/healthcheck_test.rb</verify>
  <done>Estrutura persistente de carrinho e contrato de roteamento da fase 11 estão disponíveis.</done>
</task>

<task type="auto">
  <name>Implementar controller fino e service para obter ou criar carrinho ativo</name>
  <files>marketplace_backend/app/controllers/carts_controller.rb, marketplace_backend/app/services/carts/find_or_create_active.rb, marketplace_backend/app/serializers/carts/cart_serializer.rb</files>
  <action>Implementar endpoint autenticado que usa apenas `current_user`, aplica strong parameters sem aceitar `user_id`, delega toda regra ao service e serializa resposta no envelope `{ data: { cart } }`.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/carts/find_or_create_active_test.rb</verify>
  <done>Fluxo idempotente de obtenção/criação de carrinho ativo está encapsulado em service e exposto por controller fino.</done>
</task>

<task type="auto">
  <name>Cobrir contrato do endpoint com testes de autenticação e idempotência</name>
  <files>marketplace_backend/test/integration/cart_upsert_test.rb, marketplace_backend/test/services/carts/find_or_create_active_test.rb</files>
  <action>Adicionar testes para: usuário autenticado cria carrinho ativo, chamadas repetidas retornam mesmo carrinho, requisição sem token retorna `401 token invalido` e payload inválido é rejeitado.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/cart_upsert_test.rb test/services/carts/find_or_create_active_test.rb</verify>
  <done>Contratos HTTP e regra base de idempotência ficam protegidos por regressão automatizada.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `POST /cart` exige autenticação e não usa identidade do frontend
- [ ] Usuário sem carrinho ativo recebe novo carrinho `active`
- [ ] Usuário com carrinho ativo recebe o mesmo carrinho em chamadas repetidas
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Base do carrinho ativo pronta para hardening de unicidade e isolamento tenant
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
