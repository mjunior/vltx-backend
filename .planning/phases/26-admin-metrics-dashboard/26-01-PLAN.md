---
phase: 26-admin-metrics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/app/controllers/admin/dashboard_controller.rb
  - marketplace_backend/app/services/admin_dashboard/read_summary.rb
  - marketplace_backend/config/routes.rb
  - marketplace_backend/test/integration/admin_dashboard_test.rb
  - marketplace_backend/test/integration/admin_authorization_boundary_test.rb
  - marketplace_backend/test/integration/healthcheck_test.rb
autonomous: true
requirements:
  - ADM-08
user_setup: []
must_haves:
  truths:
    - Admin consegue ler um dashboard consolidado em `/admin/dashboard`.
    - O dashboard sempre considera os últimos 30 dias sem depender de parâmetros do frontend.
    - O payload retorna `total_users`, `active_users`, `total_orders`, `orders_by_status` e volume bruto do período.
  artifacts:
    - path: marketplace_backend/app/services/admin_dashboard/read_summary.rb
      provides: agregação única e previsível das métricas administrativas
    - path: marketplace_backend/app/controllers/admin/dashboard_controller.rb
      provides: endpoint admin-only de dashboard
    - path: marketplace_backend/test/integration/admin_dashboard_test.rb
      provides: cobertura do contrato, janela fixa e agregações com zeros
  key_links:
    - from: marketplace_backend/app/services/admin_dashboard/read_summary.rb
      to: marketplace_backend/app/models/order.rb
      why: o mapa de status precisa cobrir todos os estados válidos do domínio, mesmo quando não houver pedidos
    - from: marketplace_backend/app/services/admin_dashboard/read_summary.rb
      to: marketplace_backend/app/models/user.rb
      why: o dashboard precisa distinguir `total_users` de `active_users`
    - from: marketplace_backend/app/controllers/admin/dashboard_controller.rb
      to: marketplace_backend/app/controllers/admin/application_controller.rb
      why: a leitura do dashboard deve ficar protegida pelo mesmo guard admin-only das demais rotas `/admin`
---

<objective>
Entregar o primeiro dashboard administrativo com métricas operacionais e financeiras dos últimos 30 dias.

Purpose: satisfazer `ADM-08` com um snapshot simples e estável de backoffice em `/admin/dashboard`.
Output: endpoint admin-only, service de agregação hardcoded em 30 dias e regressões de contract/boundary.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-admin-metrics-dashboard/26-CONTEXT.md
@marketplace_backend/app/controllers/admin/application_controller.rb
@marketplace_backend/app/models/order.rb
@marketplace_backend/app/models/user.rb
</context>

<tasks>

<task type="auto">
  <name>Criar service admin-only de agregação do dashboard</name>
  <files>marketplace_backend/app/services/admin_dashboard/read_summary.rb</files>
  <action>Criar um service de leitura que calcule métricas dos últimos 30 dias sem parâmetros externos: `total_users`, `active_users`, `total_orders`, `orders_by_status` com todos os status do domínio `Order`, e `gross_volume_cents` somando `subtotal_cents` dos pedidos criados na janela. Se ajudar o frontend, incluir também metadados da janela como `window_days`, `starts_at` e `ends_at`.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_dashboard_test.rb</verify>
  <done>Existe uma agregação única e determinística para o dashboard admin.</done>
</task>

<task type="auto">
  <name>Expor `GET /admin/dashboard` com contrato consolidado</name>
  <files>marketplace_backend/app/controllers/admin/dashboard_controller.rb, marketplace_backend/config/routes.rb</files>
  <action>Adicionar o endpoint atrás de `authenticate_admin!`, renderizando o payload consolidado do service de dashboard. O endpoint não deve aceitar nem depender de query params nesta fase, e deve manter a política de erro público genérico do namespace admin.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_dashboard_test.rb test/integration/healthcheck_test.rb</verify>
  <done>O painel admin consegue consumir um dashboard único e estável em `/admin/dashboard`.</done>
</task>

<task type="auto">
  <name>Fechar regressões de boundary e contrato do dashboard</name>
  <files>marketplace_backend/test/integration/admin_dashboard_test.rb, marketplace_backend/test/integration/admin_authorization_boundary_test.rb, marketplace_backend/test/integration/healthcheck_test.rb</files>
  <action>Cobrir que user token não acessa `/admin/dashboard`, que o payload sempre inclui todos os status com zero quando necessário, que os totais de usuários distinguem base total de usuários ativos e que o volume bruto considera apenas pedidos criados nos últimos 30 dias.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_dashboard_test.rb test/integration/admin_authorization_boundary_test.rb test/integration/healthcheck_test.rb</verify>
  <done>O dashboard admin fica seguro, previsível e alinhado com o contrato fechado no contexto.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/admin/dashboard` retorna métricas agregadas dos últimos 30 dias
- [ ] `orders_by_status` inclui todos os status do domínio, mesmo com zero
- [ ] user token continua sem acesso ao dashboard admin
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard contract is admin-only and panel-ready
- Window, counts, and gross volume remain deterministic and hardcoded
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
