---
phase: 04-logout-global-and-reuse-incident-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/auth/logouts_controller.rb
  - marketplace_backend/app/services/auth/jwt/access_subject.rb
  - marketplace_backend/test/integration/auth_logout_test.rb
autonomous: true
requirements:
  - SESS-05
user_setup: []
must_haves:
  truths:
    - Logout global invalida todas as refresh sessions ativas do usuário autenticado.
    - Logout válido responde `204` e permanece idempotente.
  artifacts:
    - path: marketplace_backend/app/controllers/auth/logouts_controller.rb
      provides: Endpoint `POST /auth/logout` com contrato e política de erro definidos.
    - path: marketplace_backend/config/routes.rb
      provides: Exposição da rota de logout no namespace auth.
    - path: marketplace_backend/test/integration/auth_logout_test.rb
      provides: Cobertura de sucesso, token inválido e idempotência.
  key_links:
    - from: marketplace_backend/app/controllers/auth/logouts_controller.rb
      to: marketplace_backend/app/services/auth/sessions/revoke_all.rb
      why: Logout deve usar revogação global existente em lote.
    - from: marketplace_backend/app/controllers/auth/logouts_controller.rb
      to: marketplace_backend/app/services/auth/jwt/verifier.rb
      why: Logout autenticado por access token depende de validação JWT.
---

<objective>
Implementar endpoint de logout global com autenticação por bearer token e resposta idempotente.

Purpose: Permitir encerramento explícito e seguro de todas as sessões refresh do usuário.
Output: `POST /auth/logout` retornando `204` em sucesso e `401 token invalido` em falha de token.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-logout-global-and-reuse-incident-handling/04-CONTEXT.md
@.planning/phases/04-logout-global-and-reuse-incident-handling/04-RESEARCH.md
@marketplace_backend/app/services/auth/sessions/revoke_all.rb
</context>

<tasks>

<task type="auto">
  <name>Adicionar rota e controller de logout global</name>
  <files>marketplace_backend/config/routes.rb, marketplace_backend/app/controllers/auth/logouts_controller.rb</files>
  <action>Adicionar `POST /auth/logout` em `routes.rb` e implementar controller dedicado com `Content-Type: application/json`, autenticação por `Authorization: Bearer <access_token>` e retorno `204` em sucesso.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails routes | rg "auth/logout"</verify>
  <done>Endpoint disponível com contrato público definido para sucesso.</done>
</task>

<task type="auto">
  <name>Aplicar semântica de revogação global idempotente no logout</name>
  <files>marketplace_backend/app/controllers/auth/logouts_controller.rb, marketplace_backend/app/services/auth/jwt/access_subject.rb</files>
  <action>Validar access token para extrair `user_id`, chamar `RevokeAll`, manter sucesso mesmo sem sessões ativas e retornar `401 token invalido` para token inválido/expirado/revogado.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_logout_test.rb</verify>
  <done>Logout revoga globalmente, é idempotente e mantém política de erro sem vazamento.</done>
</task>

<task type="auto">
  <name>Criar testes de integração para logout global</name>
  <files>marketplace_backend/test/integration/auth_logout_test.rb</files>
  <action>Adicionar testes cobrindo: sucesso com `204`, token inválido retornando `401 token invalido`, e idempotência (segunda chamada com contexto válido ainda sem erro de estado).</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_logout_test.rb</verify>
  <done>Testes garantem SESS-05 com comportamento de segurança consistente.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `POST /auth/logout` implementado e roteado
- [ ] Logout revoga globalmente as refresh sessions
- [ ] Erros de token no logout seguem `401 token invalido`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Logout global pronto para uso pelos clientes
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
