---
phase: 29-production-security-posture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/environments/production.rb
  - marketplace_backend/config/initializers/production_security.rb
  - marketplace_backend/test/config/production_security_posture_test.rb
  - marketplace_backend/test/integration/healthcheck_test.rb
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - Produção declara explicitamente SSL, host authorization e env validation em vez de depender de comentários/defaults.
    - O domínio Railway e o healthcheck `/up` continuam compatíveis com o hardening.
    - Falhas de configuração crítica são explícitas no boot de produção.
  artifacts:
    - path: marketplace_backend/config/environments/production.rb
      provides: baseline seguro de produção com decisões explícitas para SSL/hosts
    - path: marketplace_backend/config/initializers/production_security.rb
      provides: parsing/validation central de envs críticos de segurança
    - path: marketplace_backend/test/config/production_security_posture_test.rb
      provides: regressão automatizada para boot/config de produção
  key_links:
    - from: marketplace_backend/config/environments/production.rb
      to: marketplace_backend/railway.json
      why: o healthcheck e o host público do Railway não podem ser quebrados pelo hardening
    - from: marketplace_backend/config/initializers/production_security.rb
      to: marketplace_backend/config/initializers/cors.rb
      why: as mesmas env vars/hosts precisam alimentar a postura de CORS e host authorization
---

<objective>
Definir o baseline explícito de segurança de produção para SSL, hosts e envs críticos sem quebrar o deploy Railway.

Purpose: satisfazer a base de `SEC-01` e preservar `SEC-02` na infraestrutura já em produção.
Output: configuração de produção endurecida, helper/initializer de parsing e testes de regressão para boot/host/healthcheck.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-production-security-posture/29-CONTEXT.md
@.planning/phases/29-production-security-posture/29-VALIDATION.md
@marketplace_backend/config/environments/production.rb
@marketplace_backend/config/initializers/cors.rb
@marketplace_backend/railway.json
@marketplace_backend/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Explicitar postura de SSL, host authorization e env validation em produção</name>
  <files>marketplace_backend/config/environments/production.rb, marketplace_backend/config/initializers/production_security.rb, marketplace_backend/test/config/production_security_posture_test.rb</files>
  <action>Implementar um helper/initializer para parsear env vars de produção, exigir valores críticos, configurar `assume_ssl`, `force_ssl`, `ssl_options`, `config.hosts` e exclusões necessárias para `/up`, com comportamento seguro e observável para Railway.</action>
  <verify>cd marketplace_backend && DATABASE_URL=postgresql://localhost/marketplace_backend_test RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/config/production_security_posture_test.rb</verify>
  <done>O app deixa de depender de defaults/comentários frágeis e passa a ter baseline de produção verificável.</done>
</task>

<task type="auto">
  <name>Preservar healthcheck e compatibilidade com domínio Railway após o hardening</name>
  <files>marketplace_backend/config/environments/production.rb, marketplace_backend/test/config/production_security_posture_test.rb, marketplace_backend/test/integration/healthcheck_test.rb</files>
  <action>Garantir em testes e configuração que `/up` e os hosts públicos esperados continuem aceitos após SSL/host authorization, sem bypass amplo desnecessário.</action>
  <verify>cd marketplace_backend && DATABASE_URL=postgresql://localhost/marketplace_backend_test RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/config/production_security_posture_test.rb test/integration/healthcheck_test.rb</verify>
  <done>O baseline seguro continua operacional no fluxo de deploy e probes do Railway.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] produção define política explícita para SSL/proxy
- [ ] host authorization aceita apenas hosts configurados e compatíveis com Railway
- [ ] `/up` continua operacional após o hardening
- [ ] ausência de env crítica falha explicitamente
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Production posture is explicit and safer than the generated template
- Railway deploy assumptions remain valid
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
