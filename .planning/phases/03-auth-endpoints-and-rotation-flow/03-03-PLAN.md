---
phase: 03-auth-endpoints-and-rotation-flow
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/auth/refreshes_controller.rb
  - marketplace_backend/app/services/auth/sessions/rotate_session.rb
  - marketplace_backend/test/integration/auth_refresh_test.rb
  - marketplace_backend/test/services/auth/sessions/rotation_test.rb
autonomous: true
requirements:
  - SESS-02
  - SESS-03
user_setup: []
must_haves:
  truths:
    - Refresh token só pode ser usado uma vez; uso válido gera novo par e invalida o anterior.
    - Concorrência com mesmo token resulta em uma única vitória e segunda tentativa inválida.
  artifacts:
    - path: marketplace_backend/app/controllers/auth/refreshes_controller.rb
      provides: Endpoint de refresh com entrada estrita e respostas padronizadas.
    - path: marketplace_backend/app/services/auth/sessions/rotate_session.rb
      provides: Rotação transacional one-time com lock para evitar dupla aceitação.
    - path: marketplace_backend/test/integration/auth_refresh_test.rb
      provides: Cobertura de fluxo refresh válido/inválido e contrato HTTP.
    - path: marketplace_backend/test/services/auth/sessions/rotation_test.rb
      provides: Cobertura de concorrência e invalidade do token anterior.
  key_links:
    - from: marketplace_backend/app/controllers/auth/refreshes_controller.rb
      to: marketplace_backend/app/services/auth/jwt/verifier.rb
      why: Refresh só pode prosseguir após validação JWT de refresh token.
    - from: marketplace_backend/app/services/auth/sessions/rotate_session.rb
      to: marketplace_backend/app/models/refresh_session.rb
      why: Rotação one-time depende de lock/estado consistente na sessão persistida.
---

<objective>
Implementar endpoint de refresh com rotação transacional one-time e contrato de resposta consistente com signup/login.

Purpose: Fechar fluxo principal de continuidade de sessão sem replay.
Output: `POST /auth/refresh` com validação estrita de entrada, rotação segura e testes de concorrência.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-auth-endpoints-and-rotation-flow/03-CONTEXT.md
@.planning/phases/03-auth-endpoints-and-rotation-flow/03-RESEARCH.md
@marketplace_backend/app/services/auth/sessions/find_active_session.rb
</context>

<tasks>

<task type="auto">
  <name>Adicionar endpoint de refresh com validação estrita de payload JSON</name>
  <files>marketplace_backend/config/routes.rb, marketplace_backend/app/controllers/auth/refreshes_controller.rb</files>
  <action>Adicionar `POST /auth/refresh` aceitando apenas body JSON com `refresh_token`, rejeitando campos extras e payload inválido com `422` genérico. Tokens inválidos/revogados/expirados devem retornar `401 token invalido`.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_refresh_test.rb</verify>
  <done>Refresh endpoint aplica política de entrada e erro conforme contexto aprovado.</done>
</task>

<task type="auto">
  <name>Implementar rotação transacional one-time com lock de sessão</name>
  <files>marketplace_backend/app/services/auth/sessions/rotate_session.rb</files>
  <action>Criar serviço de rotação que verifica refresh token, trava sessão correspondente (`with_lock`), invalida token atual e atualiza a mesma linha com novo jti/hash/expiração e `rotated_at`. Segunda tentativa com token antigo deve falhar.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/auth/sessions/rotation_test.rb</verify>
  <done>Rotação impede dupla aceitação em cenário concorrente e emite novo token pair de forma atômica.</done>
</task>

<task type="auto">
  <name>Adicionar testes de integração/serviço para contrato e one-time use</name>
  <files>marketplace_backend/test/integration/auth_refresh_test.rb, marketplace_backend/test/services/auth/sessions/rotation_test.rb</files>
  <action>Criar testes cobrindo: refresh válido com novo par, reutilização do token anterior (falha 401), payload inválido (422), e corrida simulada garantindo uma vitória e uma falha.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_refresh_test.rb test/services/auth/sessions/rotation_test.rb</verify>
  <done>Testes blindam requisitos SESS-02 e SESS-03 contra regressão.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `POST /auth/refresh` implementado com entrada/erro conforme contrato
- [ ] Rotação one-time transacional impede reuse do token anterior
- [ ] Cobertura de concorrência e contrato HTTP verde
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Refresh flow pronto para fase 4 (logout/reuse incident endpoint behavior)
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
