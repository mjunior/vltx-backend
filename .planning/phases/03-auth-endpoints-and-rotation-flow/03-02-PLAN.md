---
phase: 03-auth-endpoints-and-rotation-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/config/routes.rb
  - marketplace_backend/app/controllers/application_controller.rb
  - marketplace_backend/app/controllers/auth/logins_controller.rb
  - marketplace_backend/test/integration/auth_login_test.rb
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Login autentica por email/senha e retorna token pair no mesmo contrato de signup.
    - Credenciais inválidas retornam sempre `401 credenciais invalidas` sem enumeração.
  artifacts:
    - path: marketplace_backend/app/controllers/auth/logins_controller.rb
      provides: Endpoint de login com autenticação e emissão de token pair.
    - path: marketplace_backend/config/routes.rb
      provides: Exposição de `POST /auth/login`.
    - path: marketplace_backend/test/integration/auth_login_test.rb
      provides: Cobertura de sucesso/falha e contrato HTTP do login.
  key_links:
    - from: marketplace_backend/app/controllers/auth/logins_controller.rb
      to: marketplace_backend/app/models/user.rb
      why: Login depende de autenticação `has_secure_password` no User.
    - from: marketplace_backend/app/controllers/auth/logins_controller.rb
      to: marketplace_backend/app/services/auth/sessions/create_session.rb
      why: Login deve persistir refresh session imediatamente ao emitir tokens.
---

<objective>
Entregar endpoint de login com autenticação por credencial e retorno de token pair consistente com o contrato da fase.

Purpose: Habilitar entrada autenticada segura para usuários existentes.
Output: `POST /auth/login` com status/mensagem de erro e payload de sucesso definidos.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-auth-endpoints-and-rotation-flow/03-CONTEXT.md
@.planning/phases/03-auth-endpoints-and-rotation-flow/03-RESEARCH.md
@marketplace_backend/app/models/user.rb
</context>

<tasks>

<task type="auto">
  <name>Adicionar endpoint de login e renderização de erro público para credencial inválida</name>
  <files>marketplace_backend/config/routes.rb, marketplace_backend/app/controllers/application_controller.rb, marketplace_backend/app/controllers/auth/logins_controller.rb</files>
  <action>Adicionar rota `POST /auth/login`. Implementar controller que autentica por email/senha e retorna `401 { error: 'credenciais invalidas' }` em falha. Garantir policy anti-enumeração e mensagens públicas estritamente definidas.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails routes | rg "auth/login"</verify>
  <done>Login endpoint responde com status e mensagem de erro conforme contrato da fase.</done>
</task>

<task type="auto">
  <name>Emitir token pair no login e persistir refresh session</name>
  <files>marketplace_backend/app/controllers/auth/logins_controller.rb</files>
  <action>Em login válido, emitir access+refresh, criar sessão refresh com hash+jti, e retornar envelope `data` no mesmo formato do signup (incluindo TTLs e token_type Bearer).</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_login_test.rb</verify>
  <done>Login bem-sucedido produz payload idêntico ao padrão de auth success da fase.</done>
</task>

<task type="auto">
  <name>Criar testes de integração do login para sucesso e falhas de credencial</name>
  <files>marketplace_backend/test/integration/auth_login_test.rb</files>
  <action>Adicionar testes para: sucesso com token pair, falha por senha incorreta, falha por email inexistente (mesma resposta), e validação do shape de payload para evitar regressão de contrato.</action>
  <verify>RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/auth_login_test.rb</verify>
  <done>Testes garantem autenticação correta e ausência de vazamento em erro.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `POST /auth/login` disponível e testado
- [ ] Login retorna token pair no contrato padrão
- [ ] Credenciais inválidas respondem sempre `401 credenciais invalidas`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Login alinhado ao contrato compartilhado de auth
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
