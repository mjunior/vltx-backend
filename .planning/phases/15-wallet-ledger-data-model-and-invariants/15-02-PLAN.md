---
phase: 15-wallet-ledger-data-model-and-invariants
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - marketplace_backend/app/services/wallets/ledger/append_transaction.rb
  - marketplace_backend/app/models/wallet_transaction.rb
  - marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb
  - marketplace_backend/test/models/wallet_transaction_test.rb
autonomous: true
requirements:
  - WAL-03
  - WAL-01
  - WAL-04
user_setup: []
must_haves:
    truths:
      - `balance_after_cents` é calculado no backend no mesmo fluxo atômico da inserção.
      - Escrita de ledger atualiza saldo materializado da wallet na mesma transação.
      - Em divergência runtime entre saldo materializado e ledger, fluxo corrige saldo materializado e falha a operação atual.
    artifacts:
      - path: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        provides: engine de append com cálculo server-side de `balance_after_cents`.
      - path: marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb
        provides: cobertura de atomicidade, cálculo de saldo e política fail-closed de divergência.
      - path: marketplace_backend/app/models/wallet_transaction.rb
        provides: contrato de entrada de transação alinhado ao cálculo de saldo.
    key_links:
      - from: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        to: marketplace_backend/app/models/wallet.rb
        why: service precisa sincronizar ledger e saldo materializado em transação única.
      - from: marketplace_backend/app/services/wallets/ledger/append_transaction.rb
        to: marketplace_backend/app/models/wallet_transaction.rb
        why: cálculo de `balance_after_cents` deve ser persistido junto ao insert append-only.
---

<objective>
Implementar fluxo atômico de append de transação ledger com cálculo server-side de `balance_after_cents` e sincronização segura do saldo materializado.

Purpose: Entregar WAL-03 sem abrir brecha para dados de saldo vindos de fora do backend.
Output: service de append ledger e suíte de testes de invariantes transacionais.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-wallet-ledger-data-model-and-invariants/15-CONTEXT.md
@.planning/phases/15-wallet-ledger-data-model-and-invariants/15-RESEARCH.md
@.planning/phases/15-wallet-ledger-data-model-and-invariants/15-01-PLAN.md
@.planning/phases/15-wallet-ledger-data-model-and-invariants/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Construir service de append ledger com lock/transação e cálculo server-side</name>
  <files>marketplace_backend/app/services/wallets/ledger/append_transaction.rb, marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb</files>
  <action>Implementar service que recebe `wallet`, tipo de transação e `amount_cents`; aplica lock da wallet; reconcilia runtime com ledger quando necessário; calcula `balance_after_cents`; insere em `wallet_transactions`; atualiza `wallet.current_balance_cents` na mesma transação.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/ledger/append_transaction_test.rb</verify>
  <done>Cálculo de `balance_after_cents` fica centralizado no backend com atomicidade e sem confiar em entrada externa.</done>
</task>

<task type="auto">
  <name>Aplicar política fail-closed para divergência ledger vs materializado</name>
  <files>marketplace_backend/app/services/wallets/ledger/append_transaction.rb, marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb</files>
  <action>Quando detectar mismatch runtime, recalcular saldo por `SUM` no ledger, corrigir materializado e interromper a operação retornando erro de consistência para retry seguro.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/services/wallets/ledger/append_transaction_test.rb</verify>
  <done>Fluxo mantém ledger como fonte final e evita continuar operação em estado possivelmente inconsistente.</done>
</task>

<task type="auto">
  <name>Adicionar regressão de centavos e invariantes de balance_after</name>
  <files>marketplace_backend/test/services/wallets/ledger/append_transaction_test.rb, marketplace_backend/test/models/wallet_transaction_test.rb</files>
  <action>Cobrir cenários de crédito/débito/reembolso com `amount_cents` inteiro positivo, validação de `balance_after_cents` persistido e sincronização com saldo atual da wallet após cada insert.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/models/wallet_transaction_test.rb test/services/wallets/ledger/append_transaction_test.rb</verify>
  <done>Invariantes de saldo pós-transação e domínio em centavos ficam protegidos por testes automatizados.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `balance_after_cents` nunca vem do frontend/caller
- [ ] Insert em ledger e update de saldo materializado ocorrem na mesma transação
- [ ] Divergência runtime entre ledger/materializado aplica correção + fail-closed
- [ ] Fluxo mantém domínio monetário estrito em centavos
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Fase 15 deixa base pronta para lock/idempotência das fases 16 e 17
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
