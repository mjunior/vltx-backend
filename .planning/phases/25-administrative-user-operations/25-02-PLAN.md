---
phase: 25-administrative-user-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - marketplace_backend/app/controllers/admin/user_balance_adjustments_controller.rb
  - marketplace_backend/app/services/admin/users/apply_balance_adjustment.rb
  - marketplace_backend/app/serializers/wallets/statement_transaction_serializer.rb
  - marketplace_backend/config/routes.rb
  - marketplace_backend/test/integration/admin_user_balance_adjustments_test.rb
  - marketplace_backend/test/integration/admin_authorization_boundary_test.rb
autonomous: true
requirements:
  - ADM-07
user_setup: []
must_haves:
  truths:
    - Admin ajusta saldo apenas por ledger append-only, com `credit` e `debit`.
    - Todo ajuste exige `reason` obrigatório e retorna saldo atualizado com a transação criada.
    - Ajuste administrativo não pode deixar a wallet com saldo negativo.
  artifacts:
    - path: marketplace_backend/app/services/admin/users/apply_balance_adjustment.rb
      provides: boundary admin-only para crédito/débito auditável
    - path: marketplace_backend/app/controllers/admin/user_balance_adjustments_controller.rb
      provides: endpoint `/admin/users/:id/balance-adjustments`
    - path: marketplace_backend/test/integration/admin_user_balance_adjustments_test.rb
      provides: cobertura de crédito, débito, validação e retorno do saldo atualizado
  key_links:
    - from: marketplace_backend/app/services/admin/users/apply_balance_adjustment.rb
      to: marketplace_backend/app/services/wallets/operations/apply_movement.rb
      why: o ajuste deve obrigatoriamente reaproveitar o fluxo append-only já consolidado
    - from: marketplace_backend/app/services/admin/users/apply_balance_adjustment.rb
      to: marketplace_backend/app/models/wallet_transaction.rb
      why: metadata e tipos de transação precisam continuar compatíveis com as regras do ledger
    - from: marketplace_backend/app/controllers/admin/user_balance_adjustments_controller.rb
      to: marketplace_backend/app/serializers/wallets/statement_transaction_serializer.rb
      why: o painel precisa receber o movimento criado sem inventar um contrato paralelo desnecessário
---

<objective>
Entregar ajuste administrativo de saldo com segurança de ledger e resposta auditável.

Purpose: satisfazer `ADM-07` para saldo sem permitir overwrite direto da wallet.
Output: endpoint `POST /admin/users/:id/balance-adjustments`, service admin-specific, metadata com motivo e regressões financeiras essenciais.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-administrative-user-operations/25-CONTEXT.md
@marketplace_backend/app/services/wallets/operations/apply_movement.rb
@marketplace_backend/app/services/wallets/ledger/append_transaction.rb
@marketplace_backend/app/models/wallet_transaction.rb
</context>

<tasks>

<task type="auto">
  <name>Criar service admin-only para crédito e débito de saldo</name>
  <files>marketplace_backend/app/services/admin/users/apply_balance_adjustment.rb</files>
  <action>Criar um service administrativo que encontre ou provisione a wallet do usuário, valide `transaction_type`, `amount_cents` e `reason`, e aplique `credit`/`debit` exclusivamente via `Wallets::Operations::ApplyMovement`, com metadata auditável e operation key determinístico.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_user_balance_adjustments_test.rb</verify>
  <done>Existe uma operação única e auditável para ajuste administrativo de saldo.</done>
</task>

<task type="auto">
  <name>Expor `POST /admin/users/:id/balance-adjustments` com retorno de saldo e transação</name>
  <files>marketplace_backend/app/controllers/admin/user_balance_adjustments_controller.rb, marketplace_backend/config/routes.rb, marketplace_backend/app/serializers/wallets/statement_transaction_serializer.rb</files>
  <action>Adicionar a rota sob `/admin/users/:id/balance-adjustments`, retornar `404 nao encontrado` para usuário ausente, `payload invalido` para request inválida e payload de sucesso contendo `current_balance_cents` atualizado e a transação criada com os campos úteis ao painel.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_user_balance_adjustments_test.rb</verify>
  <done>O painel consegue aplicar crédito/débito e refletir imediatamente o novo saldo.</done>
</task>

<task type="auto">
  <name>Fechar regressões financeiras e de boundary do ajuste admin</name>
  <files>marketplace_backend/test/integration/admin_user_balance_adjustments_test.rb, marketplace_backend/test/integration/admin_authorization_boundary_test.rb</files>
  <action>Cobrir crédito, débito, saldo insuficiente, `reason` ausente, tipo inválido, amount inválido e bloqueio de acesso por token de usuário comum às rotas de ajuste administrativo.</action>
  <verify>cd marketplace_backend && RBENV_VERSION=3.3.0 rbenv exec bundle exec rails test test/integration/admin_user_balance_adjustments_test.rb test/integration/admin_authorization_boundary_test.rb</verify>
  <done>O ajuste administrativo preserva as invariantes do ledger e a barreira admin/user.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] crédito e débito admin passam pelo ledger append-only
- [ ] `reason` obrigatório é persistido no metadata
- [ ] resposta retorna saldo atualizado e transação criada
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Administrative balance adjustment is auditável and ledger-safe
- Existing wallet invariants and auth boundaries remain intact
</success_criteria>

<output>
After completion, create the phase summary file following the GSD summary template.
</output>
